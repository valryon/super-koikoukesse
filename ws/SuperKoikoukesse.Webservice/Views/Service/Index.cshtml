@model SuperKoikoukesse.Webservice.Models.ServiceStateModel
<h2>Etat du webservice</h2>

<table class="table table-bordered table-condensed">
    <tr>
        <th>URL
        </th>
        <th>Méthode
        </th>
        <th>Description
        </th>
        <th>
            Appel
        </th>
    </tr>

    <tr>
        <td>@Url.Action("Exclusions", "Service")</td>
        <td>GET
        </td>
        <td>Récupération des jeux exclus post-livraison
        </td>
        <td><a class="btn btn-info" href="@Url.Action("Exclusions", "Service")">Tester</a></td>
    </tr>

    <tr>
        <td>@Url.Action("GetPlayerInfo", "Service")</td>
        <td>GET
        </td>
        <td>Récupération des info d'un joueur
        </td>
        <td><a class="btn btn-info" href="@Url.Action("GetPlayerInfo", "Service", new { gameCenterId = "Valryon" })">Tester</a></td>
    </tr>

    <tr>
        <td>@Url.Action("AddGameHistory", "Service")
        </td>
        <td>POST
        </td>
        <td>Ajouter une ligne dans l'historique des parties
        </td>
        <td>
            <form method="post" action="@Url.Action("AddGameHistory", "Service")">
                <textarea name="jsonRequest">
                    {
                       "player":"Varyon",
                       "score":10000,
                       "difficulty":0,
                       "mode":0,
                       "date":"2012-03-07 23:15:00",
                          "answers": [
		                    {"id" : 002, "result" : false},
                            {"id" : 003, "result" : true},
                            ]
                    }
                </textarea>
                <input type="submit" value="Tester" class="btn btn-primary" />
            </form>

        </td>
    </tr>
</table>
<p>
</p>
